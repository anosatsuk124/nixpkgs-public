# My nixpkgs (aka dotfiles)

## Usage

### Pre-requirements
- nix (as OS or package-manager)
- home-manager

### Installation
``` bash
git clone https://github.com/anosatsuk124/nixpkgs-public ~/.config/nixpkgs
home-manager switch
```

**You are all done!**
